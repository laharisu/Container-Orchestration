apiVersion: v1
kind: Pod
metadata:
  name: learnerreportfrontend-pods
  labels:
    name: learnerreportfrontend
spec:
  containers:
  - name: myapp
    image: <Image>
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: <Port>
